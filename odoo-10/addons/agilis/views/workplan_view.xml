<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data>

     <!-- Data file form 

    @ function: Crear y modificar registros 
    @ Descrip: Este data file define como sera el formulario, que datos seran registrados por el usuario etc. En este caso define como sera el orden en que seran registrados los campos del modelo workplan
    -->
    <record model="ir.ui.view" id="workplan_form_view">

      <field name="name">workplan.form</field>
      <field name="model">agilis.workplan</field>
      <field name="arch" type="xml">

        <form string="Work Plan Form">
          <sheet>

            <group>
              <field name="name"/>
              <field name="workplan_id"/>
            </group>        

          </sheet>
        </form>

      </field>
    </record>


    <!-- Record tree

      @Function: Listar la información en una tabla

      @Description: Record para mostrar la información en un model, donde cada field es una columna. En te caso mostramos la lista de los elementos del modelo workplan
    -->
    <record model="ir.ui.view" id="workplan_tree_view">
      <field name="name">workplan.tree</field>
      <field name="model">agilis.workplan</field>

      <field name="arch" type="xml">
        <tree string="Work Plan Tree">
          <field name="name"/>
        </tree>
      </field>

    </record>


      <!-- Tag actions.act_window 
      
        @Function: Este tag o record tiene como funcion contralar el flujo de los views a las acciones de los mismos

        @Descripcion: Una de las cosa que ahce este record es definir el modo en que las vistas seran mostradas, en este caso sera primero el <tree>, <form>, <calendar> ,y de ultimo las graficas <graph>.
      -->
      <record model="ir.actions.act_window" id="workplan_list_action">

        <field name="name">Plan de Trabajo</field>
        <field name="res_model">agilis.workplan</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
       
      </record>

      <!-- Añadimos un submenu al menu de gestion de proyectos -->
      <menuitem id="workplan_menu" 
            name="Plan de Trabajo" 
            parent="agilis_menu"
            action="workplan_list_action"/>

  </data>
</odoo>
