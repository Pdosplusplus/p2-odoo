<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data>

     <!-- Data file form 

    @ function: Crear y modificar registros 
    @ Descrip: Este data file define como sera el formulario, que datos seran registrados por el usuario etc. En este caso define como sera el orden en que seran registrados los campos del modelo ms_design
    -->
    <record model="ir.ui.view" id="ms_design_form_view">

      <field name="name">ms_design.form</field>
      <field name="model">agili.ms_design</field>
      <field name="arch" type="xml">

        <form string="Design Form">
          <sheet>

            <notebook>

              <page string="Ficha del proyecto">
                <field name="chip_id">
                  <tree string="Añade la ficha">
                    <field name="cp_activity_ids"/>
                  </tree>
                </field>
              </page>

              <page string="Cronograma del Proyecto">
                <field name="schedule_id">
                  <tree string="Añade la ficha">
                    <field name="sc_activity_ids"/>
                  </tree>
                </field>
              </page>

            </notebook>        

          </sheet>
        </form>

      </field>
    </record>

    <!-- Record tree

      @Function: Listar la información en una tabla

      @Description: Record para mostrar la información en un model, donde cada field es una columna. En te caso mostramos la lista de los elementos del modelo ms_design
    -->
    <record model="ir.ui.view" id="ms_design_tree_view">
      <field name="name">ms_design.tree</field>
      <field name="model">agili.ms_design</field>

      <field name="arch" type="xml">
        <tree string="Design Tree">
          <field name="chip_id"/>
        </tree>
      </field>

    </record>

    <!-- Tag actions.act_window 
    
      @Function: Este tag o record tiene como funcion contralar el flujo de los views a las acciones de los mismos

      @Descripcion: Una de las cosa que ahce este record es definir el modo en que las vistas seran mostradas, en este caso sera primero el <tree>, <form>, <calendar> ,y de ultimo las graficas <graph>.
    -->
    <record model="ir.actions.act_window" id="ms_design_list_action">

      <field name="name">Planificacion</field>
      <field name="res_model">agili.ms_design</field>
      <field name="view_type">form</field>
      <field name="view_mode">tree,form</field>
     
    </record>
  
    <!-- Añadimos un submenu al menu de gestion de proyectos 
    <menuitem id="ms_design_menu" 
          name="Planificacion" 
          parent="agili_menu"
          action="ms_design_list_action"/>
    -->

  </data>
</odoo>
